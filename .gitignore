# Private credentials + internal tooling (Claude Code session state,
# helper scripts, strategy notes — local only, never deployed)
.agents/
.claude/

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~

# Brand source files (large AI/PDF originals — only the optimized
# /branding/ assets get checked in for the live site)
HCS-branding/
HCS-branding 2.0/
Web Design Screenshots/
